// csv-parser.js — CSV parsing and generation via PapaParse
// TODO: copy from card-maker (T11)

/** @param {string} csvText @returns {{ data: object[], fields: string[] }} */
export function parseCsv(csvText) {
  throw new Error("Not implemented — pending T11 migration");
}

/** @param {object[]} rows @param {string[]} fields @returns {string} */
export function generateCsv(rows, fields) {
  throw new Error("Not implemented — pending T11 migration");
}
