// template-renderer.js — Mustache-like template compilation and rendering
// TODO: copy from card-maker (T11)

/** @param {string} template */
export function compileTemplate(template) {
  throw new Error("Not implemented — pending T11 migration");
}

/** @param {string} template @param {Record<string, unknown>} data */
export function renderTemplate(template, data) {
  throw new Error("Not implemented — pending T11 migration");
}

/** @param {Record<string, unknown>} row @param {object} cardType */
export function renderCard(row, cardType) {
  throw new Error("Not implemented — pending T11 migration");
}

/** @param {Record<string, unknown>} row @param {object} cardType */
export function preprocessRow(row, cardType) {
  throw new Error("Not implemented — pending T11 migration");
}
